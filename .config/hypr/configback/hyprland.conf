
# #######################################################################################
# AUTOGENERATED HYPR CONFIG.
# PLEASE USE THE CONFIG PROVIDED IN THE GIT REPO /examples/hypr.conf AND EDIT IT,
# OR EDIT THIS ONE ACCORDING TO THE WIKI INSTRUCTIONS.
# #######################################################################################


# This is an example Hyprland config file.
# Refer to the wiki for more information.
# https://wiki.hyprland.org/Configuring/Configuring-Hyprland/

# Please note not all available settings / options are set here.
# For a full list, see the wiki

# You can split this configuration into multiple files
# Create your files separately and then link them to this file like this:
# source = ~/.config/hypr/myColors.conf


################
### MONITORS ###
################

# See https://wiki.hyprland.org/Configuring/Monitors/
monitor=eDP-1,2256x1504@60,0x1080,1.6
monitor=DP-3,1920x1080@74.97,168x0,1
#monitor=,highres,auto,1,mirror,eDP-1
monitor=,highres,auto,1

###################
### MY PROGRAMS ###
###################

# See https://wiki.hyprland.org/Configuring/Keywords/

# Set programs that you use
$terminal = alacritty
$fileManager = thunar
$menu = ~/dotfiles/scripts/tofimain.sh
$browser = firefox


#################
### AUTOSTART ###
#################

# Autostart necessary processes (like notifications daemons, status bars, etc.)
# Or execute your favorite apps at launch like this:
# exec-once = $terminal
# exec-once = nm-applet &
# exec-once = waybar & hyprpaper & firefox
exec-once = fcitx5
exec-once = ~/dotfiles/scripts/random_wallpaper.sh
exec-once = wl-paste --type text --watch cliphist store 
exec-once = wl-paste --type image --watch cliphist store 
exec-once = chmod +777 -R /var/lib/iwd

#############################
### ENVIRONMENT VARIABLES ###
#############################

# See https://wiki.hyprland.org/Configuring/Environment-variables/

env = XCURSOR_SIZE,24
env = HYPRCURSOR_SIZE,24

exec = gsettings set org.gnome.desktop.interface gtk-theme "YOUR_DARK_GTK3_THEME"   # for GTK3 apps
exec = gsettings set org.gnome.desktop.interface color-scheme "prefer-dark"   # for GTK4 apps

env = QT_QPA_PLATFORMTHEME,qt6ct   # for Qt apps


#####################
### LOOK AND FEEL ###
#####################

# Refer to https://wiki.hyprland.org/Configuring/Variables/

# https://wiki.hyprland.org/Configuring/Variables/#general
general { 
    gaps_in = 5
    gaps_out = 0

    border_size = 0

    # https://wiki.hyprland.org/Configuring/Variables/#variable-types for info about colors

    # Set to true enable resizing windows by clicking and dragging on borders and gaps
    resize_on_border = false 
    # Please see https://wiki.hyprland.org/Configuring/Tearing/ before you turn this on
    allow_tearing = false
    layout = dwindle
}


# https://wiki.hyprland.org/Configuring/Variables/#decoration
decoration {
    rounding = 5

    # Change transparency of focused and unfocused windows
    active_opacity = 1
    inactive_opacity = 1
	shadow{
		enabled=true
    	range = 4
    	render_power = 3
    	color = rgba(1a1a1aee)
	}
    # https://wiki.hyprland.org/Configuring/Variables/#blur
    blur {
        enabled = true
        size = 5
        passes = 1
        vibrancy = 0.1696
    }
}

# https://wiki.hyprland.org/Configuring/Variables/#animations
animations {
    enabled = true
    # Default animations, see https://wiki.hyprland.org/Configuring/Animations/ for more
    bezier = myBezier, 0.05, 0.9, 0.1, 1.05
    animation = windows, 1, 7, myBezier
    animation = windowsOut, 1, 7, default, popin 80%
    animation = border, 1, 10, default
    animation = borderangle, 1, 8, default
    animation = fade, 1, 7, default
    animation = workspaces, 1, 6, default, slidevert
}

# See https://wiki.hyprland.org/Configuring/Dwindle-Layout/ for more
dwindle {
    preserve_split = true # You probably want this
}

# See https://wiki.hyprland.org/Configuring/Master-Layout/ for more
master {
    new_status = master
}

# https://wiki.hyprland.org/Configuring/Variables/#misc
misc { 
    force_default_wallpaper = 0 # Set to 0 or 1 to disable the anime mascot wallpapers
	disable_splash_rendering = true
	disable_hyprland_logo = true
}


#############
### INPUT ###
#############

# https://wiki.hyprland.org/Configuring/Variables/#input
input {
    kb_layout = us
    kb_variant =
    kb_model =
    kb_options = caps:swapescape
    kb_rules =

    follow_mouse = 1

    sensitivity = 0 # -1.0 - 1.0, 0 means no modification.

    touchpad {
        natural_scroll = true
    }
}

# https://wiki.hyprland.org/Configuring/Variables/#gestures
gestures {
gesture = 3, vertical, workspace,
gesture = 4, vertical, workspace,
gesture = 4, right, dispatcher, exec, $terminal
gesture = 4, left, dispatcher, exec, $browser
}

# Example per-device config
# See https://wiki.hyprland.org/Configuring/Keywords/#per-device-input-configs for more
device {
    name = razer-razer-atheris---mobile-gaming-mouse-1
#    senworkspacesitivity = -1
}


####################
### KEYBINDINGSS ###
####################

# See https://wiki.hyprland.org/Configuring/Keywords/
$mainMod = SUPER # Sets "Windows" key as main modifier

# Example binds, see https://wiki.hyprland.org/Configuring/Binds/ for more
bind = ALT, P, exec, $menu
bind = $mainMod, T, exec, $terminal
bind = $mainMod SHIFT, Q, exec, ~/dotfiles/scripts/killapp.sh
bind = $mainMod, Q, killactive
bind = $mainMod, M, fullscreen,
bind = $mainMod, F, exec, $fileManager
bind = $mainMod, V, togglefloating,
bind = $mainMod, B, exec, $browser
bind = $mainMod, I,exec, killall waybar || waybar
bind = $mainMod SHIFT, H, exec, cliphist list | tofi | cliphist decode | wl-copy
bind = $mainMod SHIFT, C, exec, hyprpicker | wl-copy

# Move focus with mainMod + arrow keys
bind = $mainMod, H, movefocus, l
bind = $mainMod, L, movefocus, r
bind = $mainMod, K, movefocus, u
bind = $mainMod, J, movefocus, d

bind = $mainMod , left, movewindow, l
bind = $mainMod , right, movewindow, r
bind = $mainMod , up, movewindow, u
bind = $mainMod , down, movewindow, d

bind = $mainMod SHIFT, up, movetoworkspace, -1
bind = $mainMod SHIFT, down, movetoworkspace, +1

bind = $mainMod, Tab, cyclenext,
bind = $mainMod, Tab, bringactivetotop,


# Example special workspace (scratchpad)
bind = $mainMod, S, togglespecialworkspace, magic
bind = $mainMod SHIFT, S, movetoworkspace, special:magic
windowrule {
name = opacityforspecialworkspace
match:workspace=s
opacity = 0.8
}

windowrule {
name = blurforspecialworkspace
match:workspace=s
no_blur = true
}

# Scroll through existing workspaces with mainMod + scroll
bind = $mainMod, mouse_down, workspace, e+1
bind = $mainMod, mouse_up, workspace, e-1

# Move/resize windows with mainMod + LMB/RMB and dragging
bindm = $mainMod, mouse:272, movewindow
bindm = $mainMod, mouse:273, resizewindow

bind = $mainMod CTRL SHIFT, R, exec, hyprshot -m region -o ~/Pictures

bind = $mainMod CTRL SHIFT, L, exec, hyprlock



bind = $mainMod ALT SHIFT CTRL,Q,exit

#trigger to type the clipboard "ultimate paste"
bind = CTRL SHIFT, A, exec, ~/dotfiles/scripts/type_clipboard.sh





# media
#bindel=, XF86AudioRaiseVolume, exec, wpctl set-volume 49 5%+ && wpctl set-volume 48 5%+
#bindel=, XF86AudioLowerVolume, exec, wpctl set-volume 49 5%- && wpctl set-volume 48 5%-
#bindl=, XF86AudioMute, exec, wpctl set-mute 49 toggle && wpctl set-mute 48 toggle
bindel=, XF86AudioRaiseVolume, exec, ~/dotfiles/scripts/VolumeControl/increase.sh
bindel=, XF86AudioLowerVolume, exec, ~/dotfiles/scripts/VolumeControl/decrease.sh
bindl=, XF86AudioMute, exec, ~/dotfiles/scripts/VolumeControl/mute.sh

bindle=, XF86MonBrightnessUp, exec, brightnessctl s +5%
bindle=, XF86MonBrightnessDown, exec, brightnessctl s 5%-

bindle=, XF86Search, exec, launchpad

bindl=, XF86AudioPlay, exec, playerctl play-pause # the stupid key is called play , but it toggles
bindl=, XF86AudioNext, exec, playerctl next
bindl=, XF86AudioPrev, exec, playerctl previous


# trigger when the switch is toggled
# trigger when the switch is turning on
bindl=,switch:on:Lid Switch,exec,hyprctl dispatch dpms off eDP-1
bindl=,switch:on:Lid Switch,exec,hyprlock
# trigger when the switch is turning off
bindl=,switch:off:Lid Switch,exec,hyprctl dispatch dpms on eDP-1


##############################
### WINDOWS AND WORKSPACES ###
##############################

# See https://wiki.hyprland.org/Configuring/Window-Rules/ for more
# See https://wiki.hyprland.org/Configuring/Workspace-Rules/ for workspace rules

# Example windowrule v1
# windowrule = float, ^(kitty)$

# Example windowrule v2
# windowrulev2 = float,class:^(kitty)$,title:^(kitty)$


xwayland {
  force_zero_scaling = true
}


# Wayland and Screensharing environment
env = MOZ_ENABLE_WAYLAND,1
env = XDG_SESSION_TYPE,wayland
env = XDG_CURRENT_DESKTOP,Hyprland
env = XDG_SESSION_DESKTOP,Hyprland

exec-once = dbus-update-activation-environment --systemd WAYLAND_DISPLAY XDG_CURRENT_DESKTOP

# Force full solid opacity on Firefox context menus
windowrule = match:class firefox, match:title ^$, opacity 1.0 override
windowrule = match:class firefox, match:title ^$, no_blur 1
windowrule = match:class firefox, match:title ^$, no_shadow 1

# Catch-all fallback for completely blank/anonymous floating menus
windowrule = match:class ^$, match:title ^$, opacity 1.0 override
windowrule = match:class ^$, match:title ^$, no_blur 1
