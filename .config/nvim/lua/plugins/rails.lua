return {
  "tpope/vim-rails",
  ft = { "ruby", "eruby", "haml", "slim" }, -- Load only for these filetypes
  config = function()
    -- Optional: Add any specific vim-rails settings here
    -- vim.g.rails_projections = { ... }
  end,
}
